apiVersion: v1
kind: Pod
metadata:
  name: mysqldb-pod
  labels:
    name: mysqldb-pod
    app: user-login-app
spec:
  containers:
    - name: mysqldb
      image: adarsha/kub_demo_mysqldb
      ports:
        - containerPort: 3306